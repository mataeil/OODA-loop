# OODA-loop — user config at repo root (contains secrets via env var references)
# Anchored with leading "/" so fixture configs under tests/ stay tracked.
/config.json

# Runtime state (generated by evolve cycles)
agent/state/*.json
agent/state/evolve/state.json
agent/state/evolve/confidence.json
agent/state/evolve/goals.json
agent/state/evolve/skill_gaps.json
agent/state/evolve/memos.json
agent/state/evolve/action_queue.json
agent/state/evolve/metrics.json
agent/state/evolve/cycle_in_progress.json
agent/state/evolve/domain-suggestions.json
agent/state/evolve/episodes.json
agent/state/evolve/principles.json
agent/state/evolve/cost_ledger.json
agent/state/external/*

# Adaptive lens files (per-domain learning, generated by evolve)
agent/state/*/lens.json
agent/state/*/lens_changelog.json

# Safety lock files
agent/safety/HALT
*.lock

# OS
.DS_Store
Thumbs.db

# IDE
.idea/
.vscode/
*.swp
*.swo
*~

# Sandbox test environment (isolated, not part of the project)
sandbox/

# Node (if user project uses it)
node_modules/
