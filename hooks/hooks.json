{
  "hooks": {
    "PreToolUse": [
      {
        "matcher": "Bash|Write|Edit|MultiEdit|NotebookEdit",
        "hooks": [
          {
            "type": "command",
            "command": "bash \"${CLAUDE_PLUGIN_ROOT:-.}/hooks/halt-guard.sh\""
          }
        ]
      }
    ],
    "SessionStart": [
      {
        "hooks": [
          {
            "type": "command",
            "command": "test -f \"${CLAUDE_PROJECT_DIR:-.}/agent/safety/HALT\" && echo '[OODA-loop] ⚠ HALT file present — autonomous cycles are paused until you remove agent/safety/HALT.' || true"
          }
        ]
      }
    ]
  }
}
